version: '3'
services:
  users-v1:
    build: .
    command: ["users-v1"]
    ports:
     - "8000:8000"

  users-v1-envoy:
    build:
        context: .
        dockerfile: Dockerfile-envoy
    command: ["envoy", "-c", "/etc/envoy/envoy.yaml"]
    ports:
      - "80:10000"
